version: '3.9'
services:
  web:
    image: book_2
    build: ./
    command: [ "python", "manage.py", "runserver", "0.0.0.0:2001" ]
    working_dir: /usr/src/app
    volumes:
      - ./:/usr/src/app
      - ./static:/usr/src/app/static
      - ./media:/usr/src/app/media
    restart: unless-stopped
    networks:
      default:
        aliases:
          - expenses
    depends_on:
      - dbb2
    ports:
      - "2001:2001"

  dbb2:
    image: postgres:latest
    ports:
      - "2032:5432"
    environment:
      - POSTGRES_DB=expenses
      - POSTGRES_USER=expenses
      - POSTGRES_PASSWORD=DANub4rH7jx8jgDpsUNSVrQk
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      default:
        aliases:
          - dbb2

  nginx:
    build: ./nginx
    volumes:
      - ./public:/app/public
    ports:
      - 2000:80
    depends_on:
      - web
